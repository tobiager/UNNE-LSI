#include <stdio.h>
#include <string.h>
char cadena[50] = " Algebra para Sistemas";
int main(){
	printf("el valor de la cadena: %s", cadena);
	printf("\nLongitud de la cadena: %i",strlen (cadena));
	
	return 0;
}

@Escribir un programa que calcule el producto entre dos números sin signo almacenados en la memoria del microprocesador:
@1Sin hacer llamadas a subrutinas, resolviendo el problema desde el programa principal.
.text
mov r1, #4
mov r2, #2
mul r3, r1, r2
.end